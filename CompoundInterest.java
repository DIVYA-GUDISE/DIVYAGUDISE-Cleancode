package Interest;
public class CompoundInterest{
	public float findCInterest(float p,int time,int r,int times)
{	
float amount=p*math.pow(1+r/times),times*time);
float compoundinterest=amount-principal;
return compoundinterest;
}
}
